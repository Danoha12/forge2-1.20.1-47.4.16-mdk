package com.trolmastercard.sexmod.entity;

import net.minecraft.world.item.ItemStack;
import net.minecraft.network.syncher.EntityDataAccessor;

/**
 * Interfaz para entidades que pueden portar el inventario custom del mod (armaduras/ropa).
 */
public interface NpcInventoryCapable {
    // Aquí puedes definir métodos comunes si los necesitas en el futuro,
    // por ahora, solo sirve como "etiqueta" para que el compilador sepa qué es la entidad.
}