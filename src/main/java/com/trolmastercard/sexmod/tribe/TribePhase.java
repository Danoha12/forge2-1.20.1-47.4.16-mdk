package com.trolmastercard.sexmod.tribe;

/**
 * TribePhase — Portado a 1.20.1.
 * * Define el estado global de actividad de una tribu de Kobolds.
 */
public enum TribePhase {
    /** La tribu está despierta, trabajando en tareas, patrullando o buscando recursos. */
    ACTIVE,

    /** La tribu está durmiendo o volviendo a sus camas. */
    REST
}